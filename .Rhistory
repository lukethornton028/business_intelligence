knitr::opts_chunk$set(echo = TRUE)
crashes_per_day = 30125 / 365
crashes_per_day
crashes_per_day = 30125 / 365
crashes_per_day
#Crashes Per Day
```{r cincy_crashes}
#Crashes Per Day
```{r cincy_crashes}
crashes_per_day = 30125 / 365
crashes_per_day
#Crashes Per Day
```{r cincy_crashes}
